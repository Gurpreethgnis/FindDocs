# FindDocs RAG Application Environment Configuration
# Copy this file to .env.local and modify the values as needed

# DocLing Server Configuration
REACT_APP_DOCLING_URL=http://localhost:35111

# Ollama Server Configuration (for production, set to actual Ollama server URL)
# In development, this uses the proxy configuration in package.json
REACT_APP_OLLAMA_URL=/api

# Optional: Enable debug logging in development
REACT_APP_ENABLE_DEBUG_LOGGING=true
